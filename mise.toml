[tools]
node = "24.14.0"
python = "3.12"

[tasks.aws-verify]
description = "Verify AWS account identity"
run = "aws sts get-caller-identity"

[env]
AWS_PROFILE = "child-daily-deutsch"
AWS_DEFAULT_REGION="us-east-1"


